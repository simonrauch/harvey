describe('day summary', () => {
  it('should print day summary');
});

describe('day rounding', () => {
  it('should round all time entries of a day');
});

describe('day modification', () => {
  it('should set new time for single time entry');
  it('should set new note for single time entry');
  it('should round time of single time entry');
  it('should delete single time entry');
});
